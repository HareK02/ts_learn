{
  "name": "ts_learn",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "scripts": {
    "build": "rimraf dist && tsc",
    "start": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "rimraf": "^5.0.5"
  }
}